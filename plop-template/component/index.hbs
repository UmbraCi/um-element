import um{{properCase name}} from './src/{{name}}.vue';

um{{properCase name}}.install = Vue =>{
    Vue.comments(um{{properCase name}}.name,um{{properCase name}})
}

export default um{{properCase name}}